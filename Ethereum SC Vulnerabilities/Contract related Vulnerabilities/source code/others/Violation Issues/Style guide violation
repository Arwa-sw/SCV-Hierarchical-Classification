Sample code:
function transferFrom (address add, uint val) return (bool succ) {
 require (val < 10 wei)
//……
}
function F(); // start name with lowercase
event evF(); // start with uppercase 
function AssertVoliation() public {
        assert(false);   }
function ReqVoliation(int256 x) public pure returns (int256) {
        require(0 < x);
        return 42; }